// Package api will hold the HTTP client this CLI uses to talk to the
// public /v1 API: capacity checks, login, deploy creation, and the
// build-log event stream. Empty for now; a later change fills it in.
package api
