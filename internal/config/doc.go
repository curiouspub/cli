// Package config will load and persist the CLI's local configuration —
// the bearer token and the API base it talks to — from the user's config
// directory. Empty for now; a later change fills it in.
package config
