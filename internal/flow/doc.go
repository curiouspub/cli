// Package flow will hold the interactive, CLI-mode renderer and the
// deploy sequence that ties pre-flight, packing, upload and log streaming
// together into what a person running `curious deploy` sees. Empty for
// now; a later change fills it in.
package flow
